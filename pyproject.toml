[project]
name = "ahacode"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openai>=3.2.0",
    "textual>=8.2.8",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "textual-dev>=1.8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]