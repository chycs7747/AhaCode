# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
dist/
*.egg-info/

# All app-generated data — never commit. One hidden folder holds config.toml
# (may carry an API key), sessions/ (private conversations), plans/, and scratch/.
.ahacode/

# Agent scratch: probe/repro logs written during a run, never source.
*.log
